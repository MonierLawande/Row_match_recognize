% ============================================================
%  ch6_experimental_evaluation.tex
%  Chapter 6: Experimental Evaluation
%  Master's Thesis -- SQL MATCH_RECOGNIZE on Pandas DataFrames
% ============================================================
% !TEX root = Thesis.tex
\chapter{Experimental Evaluation}
\label{chap:evaluation}

This chapter evaluates the proposed
\texttt{MATCH\_\allowbreak{}RECOGNIZE}
implementation across five dimensions: output correctness validated
against Trino and Oracle on selected reference queries, SQL feature
coverage of the supported SQL:2016 row-pattern subset,
performance, scalability, and developer usability.
Section~\ref{sec:eval-methodology}
describes the evaluation methodology, including the hardware
environment and the metrics used.
Section~\ref{sec:eval-dataset} presents the datasets and test
patterns.
Section~\ref{sec:eval-correctness} validates the concept example by
example on the small datasets.
Section~\ref{sec:eval-coverage} reports feature-coverage,
pattern-type-coverage, usability, and complexity-analysis results.
Section~\ref{sec:eval-performance} presents the empirical and
cross-system analysis, including the comparison with existing tools
and the performance, throughput, and memory results.

% ----------------------------------------------------------------
\section{Evaluation Methodology}
\label{sec:eval-methodology}
% ----------------------------------------------------------------

This chapter evaluates the proposed
\texttt{MATCH\_\allowbreak{}RECOGNIZE} implementation through five
complementary criteria---correctness, SQL feature coverage,
performance, scalability, and usability.
Section~\ref{subsec:eval-metrics} defines each criterion together
with the way it is measured, and
Section~\ref{subsec:eval-environment} describes the hardware and
software environment.

% ................................
\subsection{Evaluation Criteria and Metrics}
\label{subsec:eval-metrics}
% ................................

Each test case is assessed on the five complementary criteria
introduced above, defined here together with the way each is
measured.

\paragraph{Correctness.}
The result DataFrame is compared against two independent SQL
references, Trino~473 and Oracle~XE~21c, by exact row-level equality
(same rows, values, and order where deterministic).  This covers the
two small synthetic tables---\texttt{orders} (Table~\ref{tab:orders})
and \texttt{stock\_price} (Table~\ref{tab:stock_price})---and the 30
pattern--size cells of the Amazon benchmark
(Section~\ref{subsec:eval-cross-system}).  The hand-written Pandas
baseline is not a correctness oracle; it appears only in the usability
dimension (Section~\ref{subsec:eval-usability}).

\paragraph{SQL feature coverage.}
Two views are recorded.  \emph{Feature coverage} marks each main
\texttt{MATCH\_\allowbreak{}RECOGNIZE} clause---partitioning,
ordering, \texttt{PATTERN}, \texttt{DEFINE}, \texttt{MEASURES},
\texttt{SUBSET}, the output modes, skip policies, and navigation
functions~\cite{iso2016,ISO2021,monierashraf2025rowmatch}---as
\emph{supported}, \emph{partially supported}, or \emph{unsupported}
against the reference tests (Table~\ref{tab:feature_coverage}).
\emph{Pattern-type coverage} groups the test cases by row-pattern
type (simple sequences, quantified repetitions, trends, recovery,
alternation, exclusions, anchors, and \texttt{PERMUTE}); a type is
covered when at least one case matches the reference output
(Table~\ref{tab:pattern_type_coverage}).

\paragraph{Performance.}
On the Amazon UK Products dataset
(Section~\ref{subsec:eval-real-dataset}) across five pattern families
and six sizes, execution time is the \emph{client-observed end-to-end
latency} per pattern--size run---including result transfer and
DataFrame construction, so not pure engine time.  Each cell is run
five times as warmups (which let the JVM-based Trino reach steady
state) and then twenty times as measured runs; the reported time is
the \emph{mean} of the twenty, with its sample \emph{standard
deviation} (and min/max) alongside, read as mean~$\pm$~std.
Throughput is dataset size divided by this mean, averaged over a
system's 30 cells; all summaries are computed at full precision and
only displayed values are rounded, so rounded columns need not sum
exactly.  The two memory metrics measured alongside timing are defined
where they are reported (Section~\ref{subsec:eval-memory}).

\paragraph{Scalability.}
Scaling behaviour is characterised both descriptively and by an
ordinary-least-squares regression of mean execution time against row
count across the six sizes, whose coefficient of determination
($R^2$) is reported per pattern family; a value near one indicates
near-linear growth over the evaluated range.

\paragraph{Usability (developer effort).}
For each of the three worked examples
(Section~\ref{sec:eval-correctness}), the non-blank, non-comment line
count of the idiomatic Pandas solution is compared with the
single-statement \texttt{MATCH\_\allowbreak{}RECOGNIZE} query
(Table~\ref{tab:loc_comparison},
Section~\ref{subsec:eval-usability}).  Hand-written Pandas typically
needs far more code (chained \texttt{groupby}/\texttt{shift}/joins),
so the line-count reduction is a concrete measure of developer
effort~\cite{iso2016,ISO2021,melton2017sqlrpr,Python_UDF}.

% ................................
\subsection{Environment and Configuration}
\label{subsec:eval-environment}
% ................................

The experiments ran on a workstation with an Intel Core i7-11800H
processor and 64\,GB of RAM under Ubuntu 24.04.4 LTS.  For the
cross-system benchmark, each system was restricted to one CPU core and
a 32\,GB memory budget; the enforcement mechanisms are detailed in
Section~\ref{subsec:eval-complexity}.  The software stack comprised the
two baseline SQL engines---Trino~473 and Oracle~XE~21c, each run as a
Docker container---the Java~11.0.25 LTS and Python~3.12.7 runtimes, and
the core Python libraries \texttt{pandas}~2.3.3, \texttt{numpy}~2.1.3,
\texttt{antlr4-python3-\allowbreak{}runtime}~4.13.2,
\texttt{antlr4-\allowbreak{}tools}~0.2.1, and \texttt{pytest}~8.3.4.
The implementation itself uses ANTLR4 to parse Trino SQL,
\texttt{pandas} for DataFrame management, and custom AST/NFA/DFA
modules for pattern detection (Chapter~\ref{chap:methodology}).

Unless stated otherwise, all experiments use this configuration.  For
the correctness comparisons in particular, equivalent input data are
loaded into Trino, Oracle, and a Pandas DataFrame using matching
schemas, and the result sets are then compared by row order and
measure values after normalizing case-only column-name differences
between the SQL and Pandas outputs.

% ----------------------------------------------------------------
\section{Dataset and Test Patterns}
\label{sec:eval-dataset}
% ----------------------------------------------------------------

% ................................
\subsection{Datasets}
\label{subsec:eval-dataset-orders}
% ................................

This section describes, one by one, the three datasets used in the
evaluation: two small synthetic tables that can be verified by hand
(\texttt{orders} and \texttt{stock\_price}) and the large real-world
Amazon UK Products dataset, which supplies realistic data volumes (up
to 2\,million rows) for the timing and memory measurements.  The
concept is then validated example by example, dataset by dataset, in
Section~\ref{sec:eval-correctness}, and the large-scale performance
benchmark on the Amazon dataset follows in
Section~\ref{sec:eval-performance}.

\paragraph{Dataset 1: the \texttt{orders} table.}
The \texttt{orders} table supports the first worked example.  It
comprises three attributes~\cite{trino2021row}: \texttt{customer\_id}
(\texttt{VARCHAR}), which identifies the customer (two distinct
values, \texttt{cust\_1} and \texttt{cust\_2}); \texttt{order\_date}
(\texttt{DATE}), the date the order was placed (ranging from
2020-05-11 to 2020-05-18); and \texttt{price} (\texttt{INTEGER}), the
purchase amount on that date.  Each customer appears multiple times in
chronological order---\texttt{cust\_1} spans five dates and
\texttt{cust\_2} spans three---which enables focused validation of
partitioning, ordering, and pattern detection (e.g.\ sequences of
rising or falling prices per customer).  Table~\ref{tab:orders} shows
the eight records used.

\begin{table}[H]
  \centering
  \caption{Sample \texttt{orders} dataset
    (\texttt{customer\_id}, \texttt{order\_date}, \texttt{price})}
  \label{tab:orders}
  \begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|}
    \hline
    \textbf{customer\_id} & \textbf{order\_date} & \textbf{price} \\
    \hline
    cust\_1 & 2020-05-11 & 100 \\
    cust\_1 & 2020-05-12 & 200 \\
    cust\_2 & 2020-05-13 &   8 \\
    cust\_1 & 2020-05-14 & 100 \\
    cust\_2 & 2020-05-15 &   4 \\
    cust\_1 & 2020-05-16 &  50 \\
    cust\_1 & 2020-05-17 & 100 \\
    cust\_2 & 2020-05-18 &   6 \\
    \hline
  \end{tabularx}
\end{table}

\paragraph{Dataset 2: the \texttt{stock\_price} table.}
The \texttt{stock\_price} table supports the second worked example.
It represents a history of daily stock prices for market analysis and
includes three columns: \texttt{company} (the company name),
\texttt{price\_date} (the trading date), and \texttt{price} (the
stock's value on that day)~\cite{petkovic2022rpr}.  Two companies,
\texttt{A} and \texttt{B}, are recorded over ten trading days,
yielding twenty rows that support per-company trend detection.
Table~\ref{tab:stock_price} shows the data.

\begin{table}[H]
  \centering
  \caption[Stock price table]{The tabular form of the \texttt{stock\_price} table}
  \label{tab:stock_price}
  \begin{tabular}{@{}lll@{}}
    \toprule
    \texttt{Company} & \texttt{Price\_date} & \texttt{Price} \\
    \midrule
    A & 2020-10-01 & 50 \\
    B & 2020-10-01 & 89 \\
    A & 2020-10-02 & 36 \\
    B & 2020-10-02 & 24 \\
    A & 2020-10-03 & 39 \\
    B & 2020-10-03 & 37 \\
    A & 2020-10-04 & 42 \\
    B & 2020-10-04 & 63 \\
    A & 2020-10-05 & 30 \\
    B & 2020-10-05 & 65 \\
    A & 2020-10-06 & 47 \\
    B & 2020-10-06 & 56 \\
    A & 2020-10-07 & 71 \\
    B & 2020-10-07 & 50 \\
    A & 2020-10-08 & 80 \\
    B & 2020-10-08 & 54 \\
    A & 2020-10-09 & 75 \\
    B & 2020-10-09 & 30 \\
    A & 2020-10-10 & 63 \\
    B & 2020-10-10 & 32 \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Dataset 3: the Amazon UK Products dataset.}
The Amazon UK Products Dataset is a large e-commerce dataset
containing information about products listed on Amazon's UK
marketplace~\cite{asaniczka2023amazon}, commonly used for data
analysis, machine learning, and benchmarking.  It is provided as a
single CSV file with roughly 2.2\,million product records.  Each
record includes: \texttt{asin} (Amazon Standard Identification Number;
unique product ID); \texttt{title} (product name); \texttt{imgUrl}
(primary product image URL); \texttt{productURL} (canonical product
page URL); \texttt{stars} (average customer rating, 1--5,
floating-point); \texttt{reviews} (number of customer reviews,
non-negative integer); \texttt{price} (listed price in GBP, numeric);
\texttt{isBestSeller} (bestseller indicator, boolean);
\texttt{boughtInLastMonth} (approximate purchases in the last 30 days,
integer); and \texttt{categoryName} (top-level category label, e.g.\
Electronics, Books, Clothing).  This dataset is representative for
price-trend analysis, brand/feature-popularity studies, pattern
recognition, and recommender-system benchmarking.  It supplies the
data volume for the performance and scalability benchmark
(Section~\ref{subsec:eval-real-dataset}); the derived
\texttt{category} column and the construction of the six dataset
sizes are described with the benchmark patterns in
Section~\ref{subsec:eval-pattern-defs}.

% ----------------------------------------------------------------
\section{Validation of Concept}
\label{sec:eval-correctness}
% ----------------------------------------------------------------

This section validates the implementation example by example and
dataset by dataset.  Each worked example runs the same
\texttt{MATCH\_\allowbreak{}RECOGNIZE} query through the proposed engine
and the two SQL references and checks the evaluation criteria of
Section~\ref{subsec:eval-metrics}: output correctness against
Trino~473 and Oracle~XE~21c, the clauses and pattern type the query
exercises, and developer effort against a
hand-written Pandas baseline.  Beyond these three examples, the engine's
output is checked by the project's regression suite---369 test cases
across 34 files in the \texttt{tests/} directory of the
repository~\cite{monierashraf2025rowmatch}, exercising the supported
feature and pattern set against Trino-aligned expected results---and,
live against both SQL references, by the 30 pattern--size queries of
the cross-system benchmark (Section~\ref{subsec:eval-cross-system}),
where the proposed engine, Trino, and Oracle produce identical
results throughout.

% ................................
\subsection{Example 1: Dip Pattern on the \texttt{orders} Dataset}
\label{subsec:eval-dip}
% ................................

The first example detects a `dip'---three consecutive orders with
successively lower prices---on the \texttt{orders} dataset
(Dataset~1, Table~\ref{tab:orders}).  The same detection is expressed
three ways---as a Trino SQL query (Listing~\ref{lst:orders-trino}), as
a call to the proposed engine (Listing~\ref{lst:orders-mr}), and as a
hand-written Python/pandas UDF (Listing~\ref{lst:orders-udf})---and
Table~\ref{tab:sql-match-recognize-examples} confirms that all three,
together with Oracle, return the identical match.

\begin{lstlisting}[basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Trino SQL for the dip pattern]Trino SQL: create and load the \texttt{orders} table, then detect the dip pattern with \texttt{MATCH\_\allowbreak{}RECOGNIZE}.},
  label={lst:orders-trino}]
CREATE TABLE memory.default.orders (
    customer_id VARCHAR,
    order_date  DATE,
    price       INTEGER
);

INSERT INTO memory.default.orders VALUES
    ('cust_1', DATE '2020-05-11', 100),
    ('cust_1', DATE '2020-05-12', 200),
    ('cust_2', DATE '2020-05-13',   8),
    ('cust_1', DATE '2020-05-14', 100),
    ('cust_2', DATE '2020-05-15',   4),
    ('cust_1', DATE '2020-05-16',  50),
    ('cust_1', DATE '2020-05-17', 100),
    ('cust_2', DATE '2020-05-18',   6);

SELECT customer_id, start_date, end_date, bottom_price
FROM memory.default.orders
MATCH_RECOGNIZE (
  PARTITION BY customer_id
  ORDER BY order_date
  MEASURES
    A.order_date AS start_date,
    C.order_date AS end_date,
    C.price      AS bottom_price
  PATTERN (A B C)
  DEFINE
    B AS B.price < A.price,
    C AS C.price < B.price
);
\end{lstlisting}

\begin{lstlisting}[language=Python,
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Proposed MATCH\_RECOGNIZE on orders]Proposed engine: the same query as a \texttt{MATCH\_\allowbreak{}RECOGNIZE} string over an \texttt{orders} DataFrame.},
  label={lst:orders-mr}]
import pandas as pd
from src.executor.match_recognize import match_recognize

orders = pd.DataFrame({
    "customer_id": ["cust_1","cust_1","cust_2","cust_1",
                    "cust_2","cust_1","cust_1","cust_2"],
    "order_date":  ["2020-05-11","2020-05-12","2020-05-13","2020-05-14",
                    "2020-05-15","2020-05-16","2020-05-17","2020-05-18"],
    "price":       [100, 200, 8, 100, 4, 50, 100, 6],
})

query = """
SELECT customer_id, start_date, end_date, bottom_price
FROM orders
MATCH_RECOGNIZE (
  PARTITION BY customer_id
  ORDER BY order_date
  MEASURES
    A.order_date AS start_date,
    C.order_date AS end_date,
    C.price      AS bottom_price
  PATTERN (A B C)
  DEFINE
    B AS B.price < A.price,
    C AS C.price < B.price
); """
result = match_recognize(query, orders)
print(result)
\end{lstlisting}

\begin{lstlisting}[language=Python,
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Equivalent dip detection in pandas]Python UDF: the same `dip' detection as hand-written Python/pandas.},
  label={lst:orders-udf}]
import pandas as pd

def detect_dip(df, cust_col="customer_id",
               date_col="order_date", price_col="price"):
    g = df.copy()
    g[date_col] = pd.to_datetime(g[date_col])
    g = g.sort_values([cust_col, date_col]).reset_index(drop=True)
    rows = []
    for cust, grp in g.groupby(cust_col, sort=False):
        grp = grp.reset_index(drop=True)
        i = 0
        while i + 2 < len(grp):
            a, b, c = (grp.loc[i, price_col],
                       grp.loc[i + 1, price_col],
                       grp.loc[i + 2, price_col])
            if b < a and c < b:            # A B C: successively lower prices
                rows.append({
                    "customer_id":  cust,
                    "start_date":   grp.loc[i,     date_col].date(),
                    "end_date":     grp.loc[i + 2, date_col].date(),
                    "bottom_price": c,
                })
                i += 3                     # AFTER MATCH SKIP PAST LAST ROW
            else:
                i += 1
    return pd.DataFrame(rows)

result = detect_dip(orders)
print(result)
\end{lstlisting}

For the \emph{correctness} criterion, all four implementations return
the same single match, shown in
Table~\ref{tab:sql-match-recognize-examples}; the query also exercises
the \texttt{PARTITION~BY}, \texttt{ORDER~BY}, \texttt{PATTERN},
\texttt{DEFINE}, and \texttt{MEASURES} clauses and covers the
simple-sequence pattern type.

\begin{table}[H]
\centering
\caption{Dip-pattern result on the \texttt{orders} dataset: the Trino
  SQL query, the proposed engine, the Python UDF, and Oracle all
  return the identical match~\cite{monierashraf2025rowmatch}}
\label{tab:sql-match-recognize-examples}
{\small
\begin{tabular}{lllr}
\toprule
\textbf{Implementation} & \texttt{customer\_id} & \texttt{start\_date} $\rightarrow$ \texttt{end\_date} & \texttt{bottom\_price} \\
\midrule
Trino~473 (SQL)             & cust\_1 & 2020-05-12 $\rightarrow$ 2020-05-16 & 50 \\
Oracle XE~21c (SQL)         & cust\_1 & 2020-05-12 $\rightarrow$ 2020-05-16 & 50 \\
Proposed engine            & cust\_1 & 2020-05-12 $\rightarrow$ 2020-05-16 & 50 \\
Python UDF                 & cust\_1 & 2020-05-12 $\rightarrow$ 2020-05-16 & 50 \\
\bottomrule
\end{tabular}
}
\end{table}

% ................................
\subsection{Example 2: Stock-Price Analysis on the \texttt{stock\_price} Dataset}
\label{subsec:eval-stock-price}
% ................................

The second example uses the \texttt{stock\_price} table (Dataset~2,
Table~\ref{tab:stock_price}) to detect bullish momentum: a three-row
window in which the second and third rows increase relative to their
immediate predecessors.  The query partitions by company
(\texttt{PARTITION BY Company}), orders chronologically
(\texttt{ORDER BY Price\_date}), applies \texttt{PATTERN (A B C)} with
\texttt{DEFINE} predicates on \texttt{B} and \texttt{C} (variable
\texttt{A} uses the implicit always-true behaviour), and reports the
starting price, ending price, and match number in \texttt{MEASURES}.
As in Example~1, the same detection is expressed as a Trino SQL query
(Listing~\ref{lst:stock-trino}), as a call to the proposed engine
(Listing~\ref{lst:stock-mr}), and as a hand-written Python/pandas UDF
(Listing~\ref{lst:stock-pandas}); Table~\ref{tab:stock-results}
confirms that all three, together with Oracle, return the identical
three matches.  Additional use cases are available in the project
repository~\cite{monierashraf2025rowmatch}.

\begin{lstlisting}[basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Trino SQL for the stock-price pattern]Trino SQL: create and load the \texttt{stock\_price} table, then detect the increasing-window pattern with \texttt{MATCH\_\allowbreak{}RECOGNIZE}.},
  label={lst:stock-trino}]
CREATE TABLE memory.default.stock_price (
    Company    VARCHAR,
    Price_date DATE,
    Price      INTEGER
);

INSERT INTO memory.default.stock_price (Company, Price_date, Price) VALUES
    ('A', DATE '2020-10-01', 50), ('B', DATE '2020-10-01', 89),
    ('A', DATE '2020-10-02', 36), ('B', DATE '2020-10-02', 24),
    ('A', DATE '2020-10-03', 39), ('B', DATE '2020-10-03', 37),
    ('A', DATE '2020-10-04', 42), ('B', DATE '2020-10-04', 63),
    ('A', DATE '2020-10-05', 30), ('B', DATE '2020-10-05', 65),
    ('A', DATE '2020-10-06', 47), ('B', DATE '2020-10-06', 56),
    ('A', DATE '2020-10-07', 71), ('B', DATE '2020-10-07', 50),
    ('A', DATE '2020-10-08', 80), ('B', DATE '2020-10-08', 54),
    ('A', DATE '2020-10-09', 75), ('B', DATE '2020-10-09', 30),
    ('A', DATE '2020-10-10', 63), ('B', DATE '2020-10-10', 32);

SELECT *
FROM memory.default.stock_price
MATCH_RECOGNIZE (
  PARTITION BY Company
  ORDER BY Price_date
  MEASURES
    FIRST(A.Price) AS start_price,
    LAST(C.Price)  AS end_price,
    MATCH_NUMBER() AS match_id
  PATTERN (A B C)
  DEFINE
    B AS B.Price > PREV(B.Price),
    C AS C.Price > PREV(C.Price)
);
\end{lstlisting}

\begin{lstlisting}[language=Python,
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Proposed MATCH\_RECOGNIZE on stock\_price]Proposed engine: the same query as a \texttt{MATCH\_\allowbreak{}RECOGNIZE} string over a \texttt{stock\_price} DataFrame.},
  label={lst:stock-mr}]
import pandas as pd
from src.executor.match_recognize import match_recognize

data = {
    "Company": ["A","B","A","B","A","B","A","B","A","B",
                "A","B","A","B","A","B","A","B","A","B"],
    "Price_date": ["2020-10-01","2020-10-01","2020-10-02","2020-10-02",
                   "2020-10-03","2020-10-03","2020-10-04","2020-10-04",
                   "2020-10-05","2020-10-05","2020-10-06","2020-10-06",
                   "2020-10-07","2020-10-07","2020-10-08","2020-10-08",
                   "2020-10-09","2020-10-09","2020-10-10","2020-10-10"],
    "Price": [50,89,36,24,39,37,42,63,30,65,
              47,56,71,50,80,54,75,30,63,32],
}
df = pd.DataFrame(data)
df["Price_date"] = pd.to_datetime(df["Price_date"])

query = """
SELECT *
FROM stock_price
MATCH_RECOGNIZE (
  PARTITION BY Company
  ORDER BY Price_date
  MEASURES
    FIRST(A.Price) AS start_price,
    LAST(C.Price)  AS end_price,
    MATCH_NUMBER() AS match_id
  PATTERN (A B C)
  DEFINE
    B AS B.Price > PREV(B.Price),
    C AS C.Price > PREV(C.Price)
); """
result = match_recognize(query, df)
print(result)
\end{lstlisting}

\begin{lstlisting}[language=Python,
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Equivalent logic in pandas]Python UDF: the same increasing-window detection as hand-written Python/pandas.},
  label={lst:stock-pandas}]
import pandas as pd

def detect_two_consecutive_increases(
    df: pd.DataFrame,
    company_col: str = "Company",
    date_col:    str = "Price_date",
    price_col:   str = "Price",
) -> pd.DataFrame:
    gdf = df.copy()
    gdf[date_col] = pd.to_datetime(gdf[date_col])
    gdf = gdf.sort_values([company_col, date_col]).reset_index(drop=True)
    out_rows = []
    for company, g in gdf.groupby(company_col, sort=False):
        g = g.reset_index(drop=True)
        inc = g[price_col].diff() > 0
        start_mask = (inc.shift(-1).fillna(False)
                      & inc.shift(-2).fillna(False))
        candidates = [int(i) for i, ok in enumerate(start_mask)
                      if ok and i + 2 < len(g)]
        match_id, next_start_pos = 0, 0
        for i in candidates:
            if i < next_start_pos:
                continue
            match_id += 1
            out_rows.append({
                "company":     company,
                "start_price": g.loc[i, price_col],
                "end_price":   g.loc[i + 2, price_col],
                "match_id":    match_id,
            })
            next_start_pos = i + 3   # AFTER MATCH SKIP PAST LAST ROW
    return pd.DataFrame(out_rows)

result = detect_two_consecutive_increases(df)
print(result)
\end{lstlisting}

For the \emph{correctness} criterion, all four implementations return
the same three matches, shown in Table~\ref{tab:stock-results}.  This
provides evidence that partitioning, ordering, implicit
\texttt{DEFINE} behaviour, navigation with \texttt{PREV},
\texttt{MATCH\_NUMBER()}, and measure materialisation are handled
correctly for this supported pattern family.

\begin{table}[H]
\centering
\caption{Stock-price result on the \texttt{stock\_price} dataset: the
  Trino SQL query, the proposed engine, the Python UDF, and Oracle all
  return these three identical matches~\cite{petkovic2022rpr,monierashraf2025oracle}}
\label{tab:stock-results}
{\small
\begin{tabular}{rlrrr}
\toprule
\textbf{Match} & \texttt{company} & \texttt{start\_price} & \texttt{end\_price} & \texttt{match\_id} \\
\midrule
1 & A & 36 & 42 & 1 \\
2 & A & 30 & 71 & 2 \\
3 & B & 24 & 63 & 1 \\
\bottomrule
\end{tabular}
}
\end{table}

% ................................
\subsection{Example 3: V-Shape Recovery on the \texttt{stock\_price} Dataset}
\label{subsec:eval-vshape}
% ................................

The third example reuses the \texttt{stock\_price} table (Dataset~2,
Table~\ref{tab:stock_price}) but detects a different, quantified
pattern: a \emph{V-shape recovery}---a strictly falling run followed
by a strictly rising run, i.e.\ a local price minimum and its
rebound.  Unlike the fixed three-row patterns of Examples~1 and~2,
this uses the \texttt{+} quantifier twice: \texttt{PATTERN (A B+ C+)}
matches a start row \texttt{A}, one or more falling rows \texttt{B},
and one or more rising rows \texttt{C}, with the fall and rise
expressed through \texttt{PREV} navigation in \texttt{DEFINE}.  The
measures report the starting price, the bottom (last falling row), and
the recovered price (last rising row).  As before, the same detection
is expressed as a Trino SQL query (Listing~\ref{lst:vshape-trino}), as
a call to the proposed engine (Listing~\ref{lst:vshape-mr}), and as a
hand-written Python/pandas UDF (Listing~\ref{lst:vshape-udf}), and
Table~\ref{tab:vshape-results} confirms that all three, together with
Oracle, return the identical matches.

\begin{lstlisting}[basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Trino SQL for the V-shape pattern]Trino SQL: detect the V-shape recovery on the \texttt{stock\_price} table (already created and loaded in Listing~\ref{lst:stock-trino}).},
  label={lst:vshape-trino}]
SELECT *
FROM memory.default.stock_price
MATCH_RECOGNIZE (
  PARTITION BY Company
  ORDER BY Price_date
  MEASURES
    FIRST(A.Price) AS start_price,
    LAST(B.Price)  AS bottom_price,
    LAST(C.Price)  AS end_price,
    MATCH_NUMBER() AS match_id
  ONE ROW PER MATCH
  PATTERN (A B+ C+)
  DEFINE
    B AS B.Price < PREV(B.Price),
    C AS C.Price > PREV(C.Price)
);
\end{lstlisting}

\begin{lstlisting}[language=Python,
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Proposed MATCH\_RECOGNIZE V-shape]Proposed engine: the same V-shape query over the \texttt{stock\_price} DataFrame \texttt{df} of Listing~\ref{lst:stock-mr}.},
  label={lst:vshape-mr}]
from src.executor.match_recognize import match_recognize

query = """
SELECT *
FROM stock_price
MATCH_RECOGNIZE (
  PARTITION BY Company
  ORDER BY Price_date
  MEASURES
    FIRST(A.Price) AS start_price,
    LAST(B.Price)  AS bottom_price,
    LAST(C.Price)  AS end_price,
    MATCH_NUMBER() AS match_id
  ONE ROW PER MATCH
  PATTERN (A B+ C+)
  DEFINE
    B AS B.Price < PREV(B.Price),
    C AS C.Price > PREV(C.Price)
); """
result = match_recognize(query, df)
print(result)
\end{lstlisting}

\begin{lstlisting}[language=Python,
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  caption={[Equivalent V-shape detection in pandas]Python UDF: the same V-shape detection as hand-written Python/pandas.},
  label={lst:vshape-udf}]
import pandas as pd

def detect_v_shapes(df, key="Company",
                    ts="Price_date", val="Price"):
    g = df.copy()
    g[ts] = pd.to_datetime(g[ts])
    g = g.sort_values([key, ts]).reset_index(drop=True)
    out = []
    for company, grp in g.groupby(key, sort=False):
        v = grp[val].to_numpy()
        n, i, match_id = len(v), 0, 0
        while i < n - 2:
            j = i
            while j + 1 < n and v[j + 1] < v[j]:   # B+: strictly falling
                j += 1
            if j == i:                              # need at least one fall
                i += 1
                continue
            k = j
            while k + 1 < n and v[k + 1] > v[k]:    # C+: strictly rising
                k += 1
            if k == j:                              # need at least one rise
                i += 1
                continue
            match_id += 1
            out.append({
                "company":      company,
                "start_price":  int(v[i]),
                "bottom_price": int(v[j]),
                "end_price":    int(v[k]),
                "match_id":     match_id,
            })
            i = k + 1                               # AFTER MATCH SKIP PAST LAST ROW
    return pd.DataFrame(out)

result = detect_v_shapes(df)
print(result)
\end{lstlisting}

For the \emph{correctness} criterion, all four implementations return
the same three matches, shown in Table~\ref{tab:vshape-results}.
Beyond the clauses of the earlier examples, this query additionally
exercises the \texttt{+} quantifier (one-or-more) on two pattern
variables, confirming that the engine's greedy consecutive-quantifier
handling agrees with the reference engines.

\begin{table}[H]
\centering
\caption{V-shape result on the \texttt{stock\_price} dataset: the
  Trino SQL query, the proposed engine, the Python UDF, and Oracle all
  return these three identical matches~\cite{petkovic2022rpr,monierashraf2025oracle}}
\label{tab:vshape-results}
{\small
\begin{tabular}{rlrrr}
\toprule
\textbf{Match} & \texttt{company} & \texttt{start\_price} & \texttt{bottom\_price} & \texttt{end\_price} \\
\midrule
1 & A & 50 & 36 & 42 \\
2 & B & 89 & 24 & 65 \\
3 & B & 56 & 50 & 54 \\
\bottomrule
\end{tabular}
}
\end{table}

% ................................
\subsection{Usability and Developer Effort}
\label{subsec:eval-usability}
% ................................

The three worked examples of Section~\ref{sec:eval-correctness} each
express the same detection three ways---as a Trino SQL query, as a
call to the proposed engine, and as a hand-written Python/pandas
UDF---placing the declarative \texttt{MATCH\_\allowbreak{}RECOGNIZE}
form beside the two realistic alternatives an analyst faces: writing
the detection by hand in Python, or sending the data to an external
SQL engine.  Table~\ref{tab:loc_comparison} counts their non-blank,
non-comment lines: the single declarative statement is about half the
code of the equivalent UDF or less (14 versus 27 lines for the dip, 14
versus 33 for the stock-price, and 16 versus 35 for the V-shape
example), and the gap widens as the pattern grows more complex.

\begin{table}[H]
\centering
\caption{Lines-of-code comparison for the three worked examples
  (non-blank, non-comment lines)}
\label{tab:loc_comparison}
{\small
\begin{tabular}{lllr}
\toprule
\textbf{Example} & \textbf{Implementation} & \textbf{Notes} & \textbf{LOC} \\
\midrule
Dip (\texttt{orders}) & \texttt{MATCH\_RECOGNIZE} statement & single declarative query & 14 \\
Dip (\texttt{orders}) & Hand-written \texttt{pandas} UDF & loop / index / skip bookkeeping & 27 \\
\midrule
Stock (\texttt{stock\_price}) & \texttt{MATCH\_RECOGNIZE} statement & single declarative query & 14 \\
Stock (\texttt{stock\_price}) & Hand-written \texttt{pandas} UDF & \texttt{groupby}/\texttt{shift}/index bookkeeping & 33 \\
\midrule
V-shape (\texttt{stock\_price}) & \texttt{MATCH\_RECOGNIZE} statement & quantified declarative query & 16 \\
V-shape (\texttt{stock\_price}) & Hand-written \texttt{pandas} UDF & nested falling/rising run scan & 35 \\
\bottomrule
\end{tabular}
}
\end{table}

Beyond the line count, the declarative form states \emph{what} to
find---a partition key, an ordering, a \texttt{PATTERN},
\texttt{DEFINE} conditions, and a skip policy---in one statement and
lets the engine decide \emph{how}, whereas each UDF must spell out the
grouping, sorting, index bookkeeping, and skip logic by hand, where a
single off-by-one silently changes the result; this is why the UDFs
were more error-prone~\cite{Python_UDF}.  Against an external SQL
engine the value is not a new capability---\texttt{MATCH\_RECOGNIZE}
already exists in Trino and Oracle, and the engine returns identical
results---but \emph{where} it runs: in-process, directly on an
in-memory \texttt{pandas} DataFrame, avoiding the
\texttt{CREATE TABLE}/\texttt{INSERT} round-trip and keeping the
footprint small (below 1\,GB, against roughly 2.4\,GB for Oracle and
23\,GB for Trino, Section~\ref{subsec:eval-memory}), while remaining
portable back to those engines.  The trade-off is scope: the engine
supports the subset of Section~\ref{subsec:eval-feature-coverage}, not
the full SQL surface, optimiser, or distributed execution of Trino and
Oracle; and, as noted in Chapter~\ref{chap:conclusions}, this is a
code-level comparison, not a controlled user study.

% ----------------------------------------------------------------
\section{Coverage and Usability Results}
\label{sec:eval-coverage}
% ----------------------------------------------------------------

This section reports the SQL feature-coverage dimension (clause
support and pattern types) defined in
Section~\ref{sec:eval-methodology}; the usability dimension was
covered with the worked examples in
Section~\ref{subsec:eval-usability}, and the theoretical complexity
analysis follows in Section~\ref{subsec:eval-complexity}.

% ................................
\subsection{SQL Feature Coverage}
\label{subsec:eval-feature-coverage}
% ................................

This subsection reports the two coverage dimensions defined in
Section~\ref{sec:eval-methodology}: which
\texttt{MATCH\_\allowbreak{}RECOGNIZE} clauses the engine implements
(Table~\ref{tab:feature_coverage}) and which row-pattern types it
detects (Table~\ref{tab:pattern_type_coverage}), both checked against
the Trino-aligned reference tests.  In the feature table,
\emph{partially supported} means commonly used forms are implemented
and verified while less common variants remain outside the validated
scope.

\begin{table}[H]
\centering
\caption{Support status of \texttt{MATCH\_RECOGNIZE} features in the
  proposed engine}
\label{tab:feature_coverage}
{\small
\begin{tabular}{ll}
\toprule
\textbf{Feature} & \textbf{Status} \\
\midrule
\texttt{PARTITION BY} & supported \\
\texttt{ORDER BY} & supported \\
Quantifiers \texttt{* + ? \{n,m\}} & supported \\
Alternation \texttt{(A|B)} & supported \\
Grouping / nesting & supported \\
Exclusion \texttt{\{- \ldots -\}} & supported \\
Anchors \texttt{\^{}}, \texttt{\$} & supported \\
\texttt{PERMUTE} & supported \\
\texttt{DEFINE} predicates & supported \\
Navigation \texttt{PREV/NEXT/FIRST/LAST} & supported \\
\texttt{MEASURES} core aggregates & supported \\
\texttt{RUNNING}/\texttt{FINAL} semantics & supported \\
General SQL expressions & partially supported \\
\texttt{SUBSET} & supported \\
\texttt{ONE ROW PER MATCH} & supported \\
\texttt{ALL ROWS PER MATCH} & supported \\
\texttt{AFTER MATCH SKIP} modes & supported \\
\texttt{MATCH\_NUMBER()}, \texttt{CLASSIFIER()} & supported \\
\bottomrule
\end{tabular}
}
\end{table}

In summary, the engine \emph{supports} the full core of the SQL:2016
row-pattern clause: partitioning and ordering; the complete
\texttt{PATTERN} grammar (quantifiers, alternation, grouping and
nesting, anchors, exclusions, and \texttt{PERMUTE}); \texttt{DEFINE}
predicates; \texttt{SUBSET} union variables; \texttt{MEASURES} with
the navigation functions and core aggregates; \texttt{RUNNING} and
\texttt{FINAL} semantics; both output modes; all four
\texttt{AFTER MATCH SKIP} policies; and
\texttt{MATCH\_NUMBER()}/\texttt{CLASSIFIER()}.  Inside \texttt{DEFINE}
and \texttt{MEASURES} it also evaluates arithmetic, comparisons,
\texttt{IN} lists, \texttt{NULL} checks, and common scalar and string
functions (\texttt{LOWER}, \texttt{CAST}, \texttt{COALESCE},
\texttt{CASE}, \texttt{||}).  It does \emph{not} support scalar
subqueries embedded in those expressions, the
\texttt{WINDOW}/\texttt{OVER} form of row-pattern recognition
(Feature~R020), or database-level facilities such as a cost-based
optimiser and distributed execution---all of which lie outside the
R010-oriented scope of Chapters~\ref{chap:methodology}
and~\ref{chap:implementation}.

At the pattern level, Table~\ref{tab:pattern_type_coverage} lists the
row-pattern types the engine detects, each with a regex example and a
simple use case.  A type counts as covered when at least one test case
exercises it and produces output identical to the reference systems.

\begin{table}[H]
\centering
\caption{Pattern-type coverage, with a regex example and a simple use
  case for each type}
\label{tab:pattern_type_coverage}
{\small
\begin{tabular}{l l p{5.1cm} c}
\toprule
\textbf{Pattern type} & \textbf{Pattern (regex)} &
  \textbf{Simple use case} & \textbf{Covered} \\
\midrule
Simple sequence & \texttt{A+ B+} &
  a run of one category then a run of another & yes \\
Alternation & \texttt{A (B|C)+ D} &
  a fixed start, mixed intermediate states, a fixed end & yes \\
Bounded quantifier & \texttt{A\{1,5\} B* C+} &
  one to five of one event, optional filler, then one or more of
  another & yes \\
Optional parts & \texttt{A+ B? C*} &
  a required run with an optional middle and tail & yes \\
Grouping / nesting & \texttt{(A|B)+ (C\{1,3\} D*)+} &
  repeated groups nested in a larger repeated pattern & yes \\
Rising / falling trend & \texttt{A B+} &
  values rising (or falling) over several consecutive rows & yes \\
Recovery (V-shape) & \texttt{A B+ C+} &
  a fall followed by a rise---a dip that recovers & yes \\
Exclusion & \texttt{\{- A -\} B+} &
  detect a context but omit some of its rows from the result & yes \\
Anchors & \texttt{\^{}A}, \texttt{A\$} &
  the match must be at the partition start or end & yes \\
Permutation & \texttt{PERMUTE(A,B,C)} &
  the same set of events in any order & yes \\
\bottomrule
\end{tabular}
}
\end{table}

% ................................................................
\subsection{Complexity Analysis}
\label{subsec:eval-complexity}
% ................................................................

This subsection explains complexity in two complementary ways:
\emph{theoretical analysis} and \emph{empirical analysis}.  The
theoretical analysis describes how the algorithm should grow as the
input grows and is developed here; the empirical analysis reports what
was actually measured in the experiments and is presented in
Section~\ref{sec:eval-performance}.  These two views are related, but
they are not the same: theory predicts scaling, while experiments
measure the current implementation on a specific machine and dataset.

\begin{table}[H]
\centering
\caption{Theoretical and empirical views of complexity analysis}
\label{tab:complexity_analysis}
\begin{tabularx}{\textwidth}{
  >{\raggedright\arraybackslash}p{0.46\textwidth}
  >{\raggedright\arraybackslash}X}
\toprule
\textbf{Theoretical Analysis} & \textbf{Empirical Analysis} \\
\midrule
Mathematical analysis. &
Experimental measurement. \\
\midrule
Independent of hardware and software environment. &
Depends on the hardware, operating system, Python runtime,
\texttt{pandas}, and memory behavior. \\
\midrule
Describes how the running time grows as the input grows, using
asymptotic notation (Big-O, Big-$\Theta$, Big-$\Omega$). &
Uses actual execution time, throughput, and memory usage. \\
\midrule
Predicts scalability as input size grows. &
Measures real-world performance for the evaluated benchmark. \\
\bottomrule
\end{tabularx}
\end{table}

\paragraph{Theoretical analysis.}
Theoretical analysis describes how the running time grows as the
input grows, independently of the machine used to run the query (the
machine and settings for the actual measurements are those of
Section~\ref{subsec:eval-environment}).  It is expressed with
\emph{asymptotic notation}, which describes growth up to constant
factors: writing $n$ for the input size, $O(f(n))$ is an upper
bound---the cost grows \emph{no faster than} $f(n)$---while
$\Omega(f(n))$ is a lower bound (\emph{no slower than} $f(n)$) and
$\Theta(f(n))$ means both hold at once, so the cost grows
\emph{exactly like} $f(n)$.

The analysis uses five symbols, defined here once and used in every
formula below: $n$ is the number of input rows; $m$ is the pattern
size (the number of symbols in the \texttt{PATTERN} clause); $d$ is
the number of \texttt{DEFINE} variables; $k$ is the number of matches
found; and $\ell$ is the average number of rows per match.

Running a query has five stages: (1) parse the query and extract the
\texttt{MATCH\_\allowbreak{}RECOGNIZE} clauses; (2) build the
pattern's automaton (NFA, then DFA); (3) order the rows; (4) check the
\texttt{DEFINE} conditions on every row; and (5) scan for matches and
build the output.

\emph{Stages~1--2 (parse and build the automaton)} depend on the
pattern, not on the data.  The pattern is first turned into a
\emph{non-deterministic finite automaton} (NFA)---a state machine
that may follow several paths at the same time---in $O(m)$ time,
linear in the pattern size.  It is then converted into a
\emph{deterministic finite automaton} (DFA), which follows exactly
one path per row and is therefore faster to run; this conversion can
in the worst case produce $O(2^{m})$ states, but real row-pattern
queries are short and yield only a handful.  Either way this cost
depends on $m$, not on $n$, so for a fixed pattern (as in the
benchmark) it is a one-time constant, paid once however large the
data.

\emph{Stage~4 (check conditions)} reads each of the $n$ rows a fixed
number of times, costing $O(nd)$.  \emph{Stage~5 (scan and output)}
costs $O(n + k\ell)$: one pass over the rows plus the work to
assemble the $k$ matches of average length $\ell$.  \emph{Stage~3
(ordering)} is the only stage that can grow faster than linearly: a
full sort costs $O(n \log n)$, but the engine sorts only when
needed---it first checks in $O(n)$ whether the rows are already
ordered and, if so, skips the sort.

Combining the data-dependent stages gives the overall cost in two
cases:
\begin{itemize}
  \item \textbf{Best case} (input already ordered, as in the
    benchmark): the sort is skipped, giving $O(n + k\ell)$---linear
    in the number of rows, so doubling the rows roughly doubles the
    work.
  \item \textbf{Worst case} (input unordered, a real sort required):
    $O(n \log n + nd + k\ell)$, dominated by the $O(n \log n)$ sort.
\end{itemize}
The best-case linear scaling is what the measurements confirm; the
$O(n \log n)$ ordering branch is exercised on its own, with shuffled
input, in Section~\ref{subsec:eval-ordering}.  Memory use is
$O(n + k\ell)$ in both cases---the input, the per-row marks, and the
output each grow in proportion to the data.

Figure~\ref{fig:complexity_bigO} shows how the two cases grow with
the input.  The lower line is the best case: with already-ordered
input the sort is skipped and the time grows linearly in the number
of rows.  This is optimal, because it meets the $\Omega(n)$ lower
bound---no row-pattern query can do better than reading every row
once.  The upper curve is the worst case, $O(n\log n)$, reached when
the input must be sorted.  The engine's running time falls in the
shaded region between the two, depending only on whether the input
arrives ordered.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.72\textwidth]{images/complexity_bigO.png}
  \caption{Best-case and worst-case growth of the proposed engine,
    drawn to the scale of the measured results.  The best case is
    linear and meets the $\Omega(n)$ lower bound (reading every row
    once is unavoidable), so it is optimal; it applies to
    already-ordered input.  The worst case, $O(n\log n)$, applies when
    a full sort is required.  The running time falls in the shaded
    region between the two.}
  \label{fig:complexity_bigO}
\end{figure}

Beyond these, a few pattern types cost more: overlapping skip modes
(such as \texttt{SKIP TO NEXT ROW}) and conditions that depend on the
match built so far (aggregates or \texttt{CLASSIFIER()} inside
\texttt{DEFINE}) can revisit rows, up to an exponential worst case
that the engine caps with a fixed step budget
(Section~\ref{subsec:meth-exec-backtracking}).

% ----------------------------------------------------------------
\section{Empirical and Cross-System Analysis}
\label{sec:eval-performance}
\label{subsec:eval-real-dataset}
% ----------------------------------------------------------------

This section reports the measured performance of the proposed engine
and its cross-system comparison with Trino~473 and Oracle~XE~21c,
testing the scaling predicted in Section~\ref{subsec:eval-complexity}.

Performance was evaluated on the large real-world Amazon UK Products
dataset (Dataset~3, Section~\ref{subsec:eval-dataset-orders}) using
five pattern families and six input sizes (100\,K, 200\,K,
400\,K, 800\,K, 1.6\,M, and 2.22\,M rows, the last being the full
dataset), giving 30 pattern--size cells per system; the derived
\texttt{category} column and the construction of the six sizes are
detailed with the benchmark patterns in
Section~\ref{subsec:eval-pattern-defs}.  Each cell is reported as the
mean of twenty measured runs after five warmups, together with its
standard deviation.  All 90 cross-system cells completed successfully.
Over its 30 cells the proposed engine achieved an average throughput
of approximately 5\,043\,941~rows/s, and the sum of its 30 mean cell
times was about 5.96\,s (this figure aggregates the reported means,
not the total duration of all repetitions).  The results show
predictable, approximately linear scaling over the evaluated range
($R^2 \geq 0.98$ for the proposed engine's time-versus-size fit on
each of its five pattern families): simple sequences and bounded
quantifiers are the fastest families, while complex nested patterns
incur higher transition and condition-evaluation overhead.

The remainder of this section reports the empirical results across all
evaluation metrics: Section~\ref{subsec:eval-pattern-defs} defines the
five benchmark patterns; Section~\ref{subsec:eval-cross-system}
describes the resource policy together with the per-cell
execution-time and match-count results;
Section~\ref{subsec:eval-throughput} reports throughput;
Section~\ref{subsec:eval-memory} reports the two memory metrics; and
Section~\ref{subsec:eval-key-findings} summarises the key findings.

\paragraph{Positioning relative to existing systems.}
The evaluated engine is not intended to replace full SQL engines such
as Oracle or Trino, nor streaming platforms such as Flink; its design
goal is narrower---to bring a practical subset of SQL row-pattern
matching into in-memory Python analytics over \texttt{pandas}
DataFrames.  Compared with Trino and Oracle, it avoids loading data
into a database server before running
\texttt{MATCH\_\allowbreak{}RECOGNIZE}, which is convenient for
notebook and DataFrame-centred analysis, but it supports only the
subset described in Chapters~\ref{chap:methodology}
and~\ref{chap:implementation}; Trino and Oracle remain more complete
SQL systems with mature optimizers, distributed execution, and
broader standard coverage.  Compared with hand-written Pandas logic,
the SQL form is more declarative and reusable: partitioning,
ordering, pattern variables, skip behaviour, and measures are
expressed in one query rather than in custom procedural code for each
pattern.

\paragraph{Applicability and reference validation.}
The implementation applies to several sequential-analysis settings,
such as log analysis, time-series processing, and financial sequence
analysis, where recurring row patterns are important.  Across all 60
cross-system cells the proposed engine's output was compared
row-by-row against the two SQL references, Trino~473 and
Oracle~XE~21c, and matched them in every case; the hand-written
Python-UDF baseline (Section~\ref{subsec:eval-usability}) is reported
only as a usability contrast---it required substantially more code and
was more prone to errors during development~\cite{Python_UDF}---not as
a correctness oracle.

% ................................................................
\subsection{Pattern Definitions}
\label{subsec:eval-pattern-defs}
% ................................................................

{\small
\begin{longtable}{llp{0.55\textwidth}}
\caption{SQL Pattern Definitions and Detection Goals}
\label{tab:pattern_definitions} \\
\toprule
\textbf{Pattern Name} & \textbf{SQL Pattern} & \textbf{Description \& Goal} \\
\midrule
\endfirsthead
\toprule
\textbf{Pattern Name} & \textbf{SQL Pattern} & \textbf{Description \& Goal} \\
\midrule
\endhead
\bottomrule
\endfoot

simple\_sequence & \texttt{A+ B+} &
\textbf{Goal:} Detect transitions from budget to low-mid price
listings.
\newline
\textbf{Use:} Identify runs where budget products
(price~$\le$~\pounds10) are immediately followed by low-mid products
(\pounds10--25) in the listing order, useful for locating price-band
boundaries and clustered cheap listings. \\
\midrule

alternation & \texttt{A (B|C)+ D} &
\textbf{Goal:} Detect budget-to-upper-mid transitions through mixed
middle bands.
\newline
\textbf{Use:} Find sequences that start with a budget product,
continue with a mix of low-mid and mid products (\pounds10--50), and
end with an upper-mid product (\pounds50--100).  Exercises
alternation inside a quantified group. \\
\midrule

quantified & \texttt{A\{1,5\} B* C+} &
\textbf{Goal:} Detect bounded budget runs followed by mid-band
products.
\newline
\textbf{Use:} Find sequences with 1--5 budget products, optionally
followed by low-mid products, then one or more mid products
(\pounds25--50).  Exercises bounded, optional, and unbounded
quantifiers in one pattern. \\
\midrule

optional\_pattern & \texttt{A+ B? C*} &
\textbf{Goal:} Flexible budget-run detection with optional band
transitions.
\newline
\textbf{Use:} Broad matching of budget runs with an optional low-mid
product and optional mid products.  High recall; produces the
largest match sets in the benchmark. \\
\midrule

complex\_nested & \texttt{(A|B)+ (C\{1,3\} D*)+} &
\textbf{Goal:} Nested grouping of low-price runs followed by
mid/upper-mid groups.
\newline
\textbf{Use:} Detect runs of low-priced products
(price~$\le$~\pounds25) followed by one or more groups of 1--3 mid
products with optional upper-mid products.  Exercises nested
alternation combined with bounded repetition; the most expensive
pattern family. \\
\end{longtable}
}

\noindent
All patterns operate on a derived \texttt{category} column computed
from the product \texttt{price} with fixed thresholds:
$A$~= budget (price~$\le$~\pounds10),
$B$~= low-mid (\pounds10~$<$~price~$\le$~\pounds25),
$C$~= mid (\pounds25~$<$~price~$\le$~\pounds50),
$D$~= upper-mid (\pounds50~$<$~price~$\le$~\pounds100), and
$E$~= premium (price~$>$~\pounds100).
Each row also carries a sequential \texttt{seq\_id} that preserves
the original row order of the source CSV.  The six dataset sizes
(100\,K, 200\,K, 400\,K, 800\,K, 1.6\,M, and 2{,}222{,}742 rows) are
nested prefixes of the same file: the size-$n$ dataset is the first
$n$ rows of the source CSV (no random sampling, so no seed is
involved; the largest size is the entire 2{,}222{,}742-row dataset),
and \texttt{seq\_id} is assigned as $0,1,\dots,n-1$ in that order.  The benchmark queries use \texttt{ORDER~BY seq\_id} and
no \texttt{PARTITION~BY} clause, so each dataset forms a single
logical partition and the patterns match runs of consecutive rows in
this fixed order.  The sequence is
therefore a \emph{controlled benchmark sequence} derived from the
CSV listing order of the product catalog; it is not a temporal event
sequence, and no claim is made that adjacent rows are related in
time.  What matters for the benchmark is that all three systems
process the identical row sequence.
Table~\ref{tab:pattern_definitions} provides detailed
specifications of all five SQL patterns used in the evaluation,
including their formal syntax and intended detection goals.  Each
pattern represents a different complexity level and use case, ranging
from simple sequential matching to complex nested structures with
quantifiers and alternation operators.

\paragraph{Benchmark setup and resource policy.}
The empirical analysis measures the implemented systems directly.  In
this chapter, the final benchmark consists of 90 successful
benchmark cells: three systems, five pattern families, and six
dataset sizes from 100\,K to 2.22\,M rows, where each cell is the mean
of twenty measured executions after five warmups (1{,}800 measured
executions in total).  The proposed Pandas engine, Trino~473, and
Oracle~XE~21c were evaluated on the same prepared CSV subsets (nested
prefixes of the source dataset) and the same
\texttt{MATCH\_\allowbreak{}RECOGNIZE} query bodies.

The resource policy was enforced as follows.  The three systems ran
strictly sequentially, one at a time, with the other database
containers stopped.  The Pandas engine was pinned to CPU core~0 via
\texttt{sched\_setaffinity}, with the numerical-library
thread pools (\texttt{OMP}, \texttt{OpenBLAS}, \texttt{MKL},
\texttt{NumExpr}) limited to one thread; the database containers
were pinned to the same core with
\texttt{docker update -{}-cpuset-cpus=0} combined with a one-CPU
quota (\texttt{-{}-cpus=1}).  Every system was given the same
nominal 32\,GB memory budget, enforced by the mechanism native to
each deployment: an address-space \texttt{rlimit} for the Python
process, and a container memory limit with \texttt{-{}-memory-swap}
equal to the limit (no extra swap) for the databases.  These
mechanisms are equivalent in budget but not identical in enforcement
semantics.  The 32\,GB figure is therefore an
\emph{engine/container} memory budget: it caps the Pandas engine
process and each database container.  The budget is below the
workstation's 64\,GB of installed RAM, so it is an actually enforced
cap.  For the database runs, the separate Python client that issues
the query and materializes the result DataFrame is pinned to core~0
via \texttt{sched\_setaffinity} (the same call that pins the Pandas
engine), applied once to the benchmark process at start-up; container
pinning alone would not constrain this host-side client, and the
client's own memory is not itself placed under the 32\,GB
\texttt{rlimit}.  Client and database engine therefore time-share the
single core~0, and, correspondingly, the container footprint values
cover only the database process and exclude this client, whose
result-construction memory is counted in neither database memory
metric.
Reported times are client-observed wall-clock
durations of query execution including result retrieval, measured
tightly around the query call so that sampling overhead was
minimized.  The timer
stops only after the client has fully materialized the result set
(fetching all rows and constructing the client-side DataFrame), and
every system returns the same normalized result columns and row
counts for each cell (Table~\ref{tab:match_counts}), so result
retrieval is included in the timing on equal terms.  Database
loading time is excluded from the reported query times; the
measurements report query execution after the data has been loaded
into the target system.

The theoretical model explains the expected growth, while the empirical
results show the current implementation's observed behavior.  The
Pandas engine shows stable throughput across the evaluated range,
which is consistent with a scan-dominated execution path for these
fixed-size patterns, and records the lowest average execution time of
the three systems on every one of the 30 pattern--size cells; the
per-cell figures and the per-family speedups are reported below and
summarised in Section~\ref{subsec:eval-key-findings}.  Memory is
reported using the two metrics defined in
Section~\ref{subsec:eval-metrics}: the engine-reported
system-native \emph{query-memory indicators}, and the
\emph{operational footprint}, which documents the cost of the
resident process or database container and is reported separately.

% ................................................................
\subsection{Cross-System Comparison Under a Common Resource Budget}
\label{subsec:eval-cross-system}
% ................................................................

The benchmark artifacts were saved externally for reproducibility in
\texttt{Performance/cross\_system\_matrix}: one result file per system
(\texttt{pandas\_results.csv}, \texttt{trino\_results.csv}, and
\texttt{oracle\_results.csv}), the shared input datasets, the saved SQL
query files, and combined summary files.  The common query body for
each pattern is stored once and the per-system query files differ only
in the source table reference required by each system.

Because runtime and memory depend directly on how many matches each
pattern produces, the match count for every pattern--size combination
is reported in Table~\ref{tab:match_counts}
(Appendix~\ref{app:benchmark-tables}).  All queries use
\texttt{ONE ROW PER MATCH}, so the match count equals the number of
returned result rows; the counts are identical across the three
systems (this is exactly what the correctness comparison verifies).
\texttt{optional\_pattern} produces the largest output sets and
\texttt{alternation} the smallest, which explains much of the
per-pattern variation in the time and memory tables that follow.

% Final run (each cell = 5 warmup + 20 measured; 1 core, 32GB; sizes 100K-2.22M; mean +/- sample std)
% pandas: total=5.96, avg=0.20, avgthr=5{,}043{,}941, minthr=1{,}791{,}628, maxthr=8{,}055{,}323, maxqmem=173.81, maxfmem=665.63, medCV=8.8%
% Trino~473: total=57.88, avg=1.93, avgthr=609{,}122, minthr=153{,}963, maxthr=1{,}009{,}725, maxqmem=41.50, maxfmem=23{,}225.34, medCV=5.9%
% Oracle XE~21c: total=28.21, avg=0.94, avgthr=1{,}258{,}336, minthr=359{,}113, maxthr=1{,}802{,}506, maxqmem=58.19, maxfmem=2{,}523.6, medCV=2.1%

Figure~\ref{fig:summary_by_pattern} gives the high-level picture by
pattern family, averaged across all six dataset sizes; the underlying
values are in Table~\ref{tab:pattern_summary} (throughput and time) and
Table~\ref{tab:avg_time_by_pattern} (time only) in
Appendix~\ref{app:benchmark-tables}.  The proposed engine records the
highest average throughput and the lowest average time on all five
pattern families in this run, against both database systems.  The
largest gap is on \texttt{complex\_nested}, the family where Trino
shows by far its largest relative cost (5.04\,s versus 0.40\,s on
average, about 12.6$\times$).  This indicates that both implementation
strategy and pattern structure affect the measured runtime.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{images/viz_summary_by_pattern.png}
  \caption[Performance summary by pattern]{Average throughput (left,
    higher is better) and average execution time (right, lower is
    better) by pattern family and system, each averaged over the six
    dataset sizes.  Underlying values in
    Table~\ref{tab:pattern_summary} and
    Table~\ref{tab:avg_time_by_pattern}
    (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:summary_by_pattern}
\end{figure}

Figure~\ref{fig:summary_by_size} groups the same measurements by
dataset size; the underlying values are in
Table~\ref{tab:size_summary} (throughput and time) and
Table~\ref{tab:avg_time_by_size} (time only) in
Appendix~\ref{app:benchmark-tables}.  The proposed engine records the
lowest average time at every size, and its throughput remains high
(per-size averages of roughly 3.6\,M--5.6\,M rows/s) as the input
increases.  Relative to a fully-warmed Trino, the engine's per-size
averages are lower at all six sizes, by a factor of approximately
7.3--10.6$\times$.  Relative to Oracle, the engine's per-size averages
are also lower across the evaluated range, by a factor of
approximately 3.1--5.2$\times$, using the per-size averages of
Table~\ref{tab:size_summary}.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{images/viz_summary_by_size.png}
  \caption[Performance summary by dataset size]{Average throughput
    (left, higher is better) and average execution time (right, lower
    is better, log scale) by dataset size and system, each averaged
    over the five pattern families.  Underlying values in
    Table~\ref{tab:size_summary} and Table~\ref{tab:avg_time_by_size}
    (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:summary_by_size}
\end{figure}


Figure~\ref{fig:execution_times} plots the detailed execution-time
matrix on log--log axes, one panel per pattern; the full per-cell
numbers (mean~$\pm$~standard deviation) are in
Table~\ref{tab:execution_times}
(Appendix~\ref{app:benchmark-tables}).  The proposed engine grows
predictably with input size and is the lowest curve in every panel at
every size; the three systems track near-parallel straight lines (near
power-law growth), and the relative cost of each pattern depends on the
matcher path selected for it.  The gap widens most for
\texttt{complex\_nested}---the most expensive pattern family for Trino
and Oracle---where the databases bend upward past $400{,}000$ rows
while the engine stays close to linear.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{images/execution_times.png}
  \caption[Execution time by pattern, size, and system]{Execution time
    versus dataset size by pattern and system (log--log axes), plotting
    the data of Table~\ref{tab:execution_times}.  The proposed engine
    (blue) is the lowest curve in every panel; the margin over Trino and
    Oracle is widest for \texttt{complex\_nested}.}
  \label{fig:execution_times}
\end{figure}

% ................................................................
\subsection{Throughput by Pattern, Size, and System}
\label{subsec:eval-throughput}
% ................................................................

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{images/viz_throughput.png}
  \caption[Throughput by pattern, size, and system]{Throughput (rows/s) versus dataset size by pattern and system (log--log axes), one panel per pattern.  Higher is better, so the proposed engine is the highest curve in every panel.  The full matrix is in Table~\ref{tab:throughput} (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:throughput}
\end{figure}

Figure~\ref{fig:throughput} shows throughput for every pattern--size
combination, with the full matrix in Table~\ref{tab:throughput}
(Appendix~\ref{app:benchmark-tables}).  Throughput is computed as
dataset size divided by measured execution time, so higher values
indicate faster evaluation.

% ................................................................
\subsection{Memory Metrics by Pattern, Size, and System}
\label{subsec:eval-memory}
% ................................................................

Memory is reported using the two metrics defined in
Section~\ref{subsec:eval-metrics}.  \emph{Query memory}
(Figure~\ref{fig:query_memory}; Table~\ref{tab:memory_cache} in
Appendix~\ref{app:benchmark-tables}) is the peak additional memory
attributable to executing the measured query, taken from each
system's own memory accounting: process RSS peak delta for the
Pandas engine, the per-query \texttt{peakMemoryBytes} counter for
Trino, and the session PGA high-water delta of a fresh session for
Oracle.  All three exclude the stored table data and the idle
engine.  They are, however, \emph{system-native indicators} rather
than one instrument: RSS counts operating-system pages, Trino counts
reserved user memory, and Oracle counts allocated work-area memory.
The values should therefore be read as order-of-magnitude and
per-system trend evidence, not as directly comparable measurements
of identical quantities.
\emph{Footprint memory} (Figure~\ref{fig:footprint_memory};
Table~\ref{tab:memory_footprint} in
Appendix~\ref{app:benchmark-tables}) is the
absolute process RSS peak for the Pandas engine and the Docker
container peak for Trino and Oracle; it documents the practical
deployment cost of the resident engine and is \emph{not} a
per-query or algorithm-level metric.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{images/viz_query_memory.png}
  \caption[Query memory by pattern, size, and system]{Query-memory indicator (MB) versus dataset size by pattern and system (log--log axes).  These are system-native indicators and not directly comparable instruments (Section~\ref{subsec:eval-memory}).  The full values are in Table~\ref{tab:memory_cache} (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:query_memory}
\end{figure}

Figure~\ref{fig:query_memory} shows that the average query-memory
indicators generally increase with input size, as expected for a
per-query working set.  For the most memory-intensive pattern
families at the largest input (the full 2.22\,M rows), the three
indicators fall between roughly 41 and 174\,MB; this range describes
only those worst-case cells, not the benchmark as a whole, where most
readings are far smaller.  At 2.22\,M rows the Pandas engine's
vectorized execution path trades additional working memory for its
lower execution times on three families---its largest indicator is
on \texttt{optional\_pattern} (173.81\,MB), followed by
\texttt{quantified} (164.31\,MB) and \texttt{simple\_sequence}
(160.03\,MB), each above Oracle's 58.19\,MB and Trino's roughly
41.5\,MB---whereas on the other two families its readings stay at or
below the databases' (\texttt{complex\_nested} at 42.01\,MB and
\texttt{alternation} at the measurement floor, against Trino's
41.4--41.5\,MB and Oracle's 58.19\,MB).

Values of 0.00 in Table~\ref{tab:memory_cache} do not mean that a
query executed without memory; they mean that the query's working
set stayed below the resolution of the corresponding counter.
Oracle pre-allocates an initial block of PGA to every new session at
connect time, so a small query whose \texttt{MATCH\_\allowbreak{}RECOGNIZE}
work areas fit inside that initial allocation never raises the
session's PGA high-water mark, and the measured delta is zero.
Likewise, a Pandas run that fits into pages already resident in the
Python process produces no RSS growth.  Readings close to this floor
are also quantized to the runtime's allocation granularity (Oracle,
for example, extends session PGA in small fixed-size chunks), so
near-floor values should not be compared against each other.  At larger input sizes the database counters
grow steadily with the input, while the RSS-based Pandas readings
fluctuate more (values can rise, fall, or return to zero between
neighboring cells) because RSS deltas depend on page reuse and
garbage-collection timing inside the Python process; the overall
upward trend beyond the measurement floor is what the metric is
intended to show.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{images/viz_footprint_memory.png}
  \caption[Footprint memory by pattern, size, and system]{Footprint memory (MB) versus dataset size by pattern and system (log--log axes).  The Pandas engine is a single process; the Trino and Oracle values are container peaks.  The full values are in Table~\ref{tab:memory_footprint} (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:footprint_memory}
\end{figure}

Figure~\ref{fig:footprint_memory} shows the practical deployment
footprints, whose boundaries differ by construction: the Pandas value
is one Python process including the loaded input DataFrame, and peaks
at about 0.67\,GB at the full 2.22\,M rows, whereas the database
values cover entire Docker containers---Trino's grows to about 23\,GB
(its memory connector holds the benchmark table in the JVM heap) and
Oracle's stays near 2.4--2.5\,GB.  The database figures are per-run
samples of a container that persists across dataset sizes within a
pass (restarted only between systems), so they carry allocations
retained from earlier sizes and must not be read as independent
per-size footprints; restarting the container before each size would
give per-size figures and is a recommended refinement for future
runs.  These values document deployment cost; per-query comparisons
should use Table~\ref{tab:memory_cache}.

% ................................................................
\subsection{Key Findings}
\label{subsec:eval-key-findings}
% ................................................................

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{images/viz_overall_stats.png}
  \caption[Overall benchmark statistics by system]{Overall benchmark statistics by system: average execution time, average throughput, maximum query-memory indicator, and maximum footprint (log scale).  The underlying values are in Table~\ref{tab:overall_stats} (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:overall_stats}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.82\textwidth]{images/viz_avg_memory_by_size.png}
  \caption[Average query memory by dataset size]{Average query-memory indicator by dataset size and system (log--log).  The underlying values are in Table~\ref{tab:avg_memory_by_size} (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:avg_memory_by_size}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.82\textwidth]{images/viz_avg_footprint_by_size.png}
  \caption[Average footprint memory by dataset size]{Average footprint memory by dataset size and system (log--log).  The underlying values are in Table~\ref{tab:avg_footprint_by_size} (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:avg_footprint_by_size}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{images/viz_correctness.png}
  \caption[Cross-system correctness by pattern]{Cross-system correctness by pattern: for every pattern, Trino and Oracle reproduced the proposed engine's normalized output on all six dataset sizes.  The tabular form is in Table~\ref{tab:cross_system_correctness} (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:correctness}
\end{figure}


Figures~\ref{fig:overall_stats}--\ref{fig:correctness} summarize the
main findings, with the underlying
Tables~\ref{tab:overall_stats}--\ref{tab:cross_system_correctness} in
Appendix~\ref{app:benchmark-tables}.  All 90 benchmark cells completed
successfully, and Trino and Oracle produced output identical to the
proposed engine's normalized output for all 30 pattern--size
combinations each---all three systems produced identical result
sets.  The proposed Pandas engine had the lowest average
execution time in this benchmark (0.20\,s), ahead of Oracle
(0.94\,s) and a fully-warmed Trino (1.93\,s): it recorded the
lowest time on all 30 pattern--size cells, faster than Trino by
roughly 6--13$\times$ per family and faster than Oracle by roughly
2.7--5.5$\times$, with the widest margins on \texttt{complex\_nested}
(where Trino averages 5.04\,s against the engine's 0.40\,s).  The
system-native
query-memory indicators of all three systems fall in the same order
of magnitude, reaching at most 41--174\,MB for the most
memory-intensive patterns at the full 2.22\,M rows---where the
engine's largest indicators (\texttt{optional\_pattern} at
173.81\,MB and \texttt{quantified} at 164.31\,MB)
exceed Oracle's 58.19\,MB and Trino's roughly 41.5\,MB, while on
\texttt{alternation} and \texttt{complex\_nested} its readings stay
at or below the databases'---and the
operational
footprints differ substantially: the Pandas engine peaks at about
0.67\,GB as a single Python process, the Oracle container stays near
2.5\,GB (about 3.8$\times$ the Pandas peak), and the Trino
container reaches about 23\,GB under its memory-connector setup.
Across the twenty measured runs per cell, run-to-run variability is
low: the median \emph{coefficient of variation} (the standard
deviation as a fraction of the mean) is about 9\% for the Pandas
engine, 6\% for Trino, and 2\% for Oracle; the five warmup runs
remove the cold-start outliers that a single warmup had left behind
for the JVM-based Trino.  These
results are not presented as a claim that one system is universally
better; they show the trade-off observed under the selected
workload, data sizes, and resource configuration.

\paragraph{Why the proposed engine is faster on this workload.}
The measured advantage is a direct consequence of specialization,
not of a generally superior execution model, and it should be read
together with the scope limits stated at the start of this section.
Four factors explain it.  \emph{First}, the engine is a
single-purpose row-pattern matcher: it carries none of the
general-purpose query-planning, catalog, transaction, logging, or
distributed-coordination machinery that Trino and Oracle must run on
every query, so it has almost no fixed per-query overhead to amortize
at these input sizes.  \emph{Second}, execution is entirely
in-process over \texttt{pandas}/NumPy arrays: there is no client
protocol, no result serialization, and no cross-process data movement
between a client and a server, all of which the database paths pay
even when the table is already loaded.  \emph{Third}, the
implementation compiles each query to a narrow fast path tailored to
the evaluated workload---\texttt{ONE ROW PER MATCH}, a single logical
partition, and row-local \texttt{DEFINE} predicates.  These predicates
are evaluated as vectorized column operations (Chapter~\ref{chap:implementation}),
category comparisons are reduced to integer-code checks, the
deterministic automaton is walked from precompiled per-state decision
tables that skip whole constant-mask runs in one step, and the output
columns and running measures are assembled with grouped NumPy
reductions rather than one Python object per matched row.  Each of
these turns a per-row Python operation into an amortized array
operation, which is where the bulk of the speedup arises.  A fourth
factor---that the ordered benchmark input lets the engine skip
sorting entirely---is examined on its own in
Section~\ref{subsec:eval-ordering}, which confirms the ranking is
unchanged once that shortcut is removed.  This speedup holds only for
the supported feature subset and single-node in-memory setting, and
it costs more per-query working memory on the sequence-style families
(Figure~\ref{fig:query_memory}).

Figure~\ref{fig:relative_comparison} condenses the trade-off into a
single relative view.  It shows the shape of the result compactly: a
large execution-time and throughput advantage and a far smaller
operational footprint, obtained at the cost of a per-query memory
indicator that is comparable to the databases' rather than below it
(about 41\% above Trino's counter and level with Oracle's).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.9\textwidth]{images/viz_relative_comparison.png}
  \caption[Proposed engine relative to each SQL engine]{Proposed engine relative to each SQL engine, averaged over the 30 benchmark cells (symmetric-log axis).  Lower is better for execution time and both memory metrics; higher is better for throughput.  The underlying values are in Table~\ref{tab:relative_comparison} (Appendix~\ref{app:benchmark-tables}).}
  \label{fig:relative_comparison}
\end{figure}

% ................................................................
\subsection{Best-Case and Worst-Case Sorting}
\label{subsec:eval-ordering}
% ................................................................

The benchmark reported so far orders by a sequential \texttt{seq\_id}
whose values are already monotonic in the input, so the proposed
engine takes the \emph{ordered-input} branch of
Section~\ref{subsec:eval-complexity}: a single $O(n)$ monotonicity
check lets it skip \texttt{sort\_values} entirely.  That is realistic
for append-ordered DataFrames, but it exercises only the $O(n)$ side
of the ordering step and gives the engine a shortcut the two database
systems never take, since a SQL table has no guaranteed row order and
\texttt{ORDER~BY} therefore sorts on every query.  To measure the
other side---the $O(n\log n)$ branch the complexity analysis
predicted but the ordered benchmark never triggered---the identical
matrix was rerun with the physical row order of every input
\emph{shuffled} (deterministically, per size), which forces a genuine
sort in all three systems.  Because the logical order after sorting is
unchanged, the match results and counts are byte-identical across the
two scenarios (verified for all 90 cells), so any time difference is
attributable to sorting alone.  Scenario~A (ordered) is thus the
best case and Scenario~B (shuffled) the fair worst case for the
ordering step, and the two bracket the engine's realistic
performance.  Table~\ref{tab:ordering_scenarios} reports the average
per system.

\begin{table}[H]
\centering
\caption{Average execution time under ordered (Scenario~A) and
  shuffled (Scenario~B) input, over all 30 pattern--size cells per
  system.  Scenario~B forces a genuine sort in every system; match
  counts are identical between the two scenarios.}
\label{tab:ordering_scenarios}
\begin{tabular}{lrrr}
\toprule
\textbf{System} & \textbf{A ordered (s)} & \textbf{B shuffled (s)}
  & \textbf{Slowdown} \\
\midrule
Proposed Pandas engine & 0.20 & 0.27 & $1.34\times$ \\
Trino~473              & 1.93 & 2.18 & $1.13\times$ \\
Oracle XE~21c          & 0.94 & 1.09 & $1.16\times$ \\
\bottomrule
\end{tabular}
\end{table}

Three observations follow.  \emph{First}, the proposed engine incurs
the largest relative slowdown ($1.34\times$) because it is the only
system that skips sorting in the ordered case; the penalty is
therefore the full cost of the sort it now performs.  It is widest on
the sequence-style families, where matching is so cheap that the sort
dominates (\texttt{simple\_sequence} $1.56\times$,
\texttt{quantified} $1.48\times$), and narrowest on
\texttt{complex\_nested} ($1.20\times$), where the automaton walk
dominates and the sort is a smaller share of the total.
\emph{Second}, the databases slow down far less ($1.13$--$1.16\times$)
because they sort in \emph{both} scenarios; they benefit only from the
fact that sorting already-ordered rows is cheaper than sorting
shuffled ones (fewer comparisons and moves, better cache locality),
not from skipping the sort.  \emph{Third}, and most important, the
ranking is unchanged: at the full 2.22\,M rows the engine remains
about $4.2\times$ faster than Oracle and $8.1\times$ faster than Trino
under shuffled input (against $4.8\times$ and $9.6\times$ under
ordered input).  The engine's advantage is therefore not an artifact
of the ordered-input shortcut---it survives when every system must
sort.

These measurements also validate the complexity model of
Section~\ref{subsec:eval-complexity} directly.  The incremental cost
of Scenario~B over Scenario~A isolates the ordering step, and it
behaves exactly as an $O(n\log n)$ sort should: at the larger sizes it
becomes essentially \emph{pattern-independent} (its spread across the
five families narrows to a coefficient of variation of about
13\%\ at 800\,K rows and above), confirming that it is the
per-query sort rather than any matching work; and its per-row cost
\emph{rises} with input size, from about 53 to 83\,ns per row between
100\,K and 2.22\,M rows, which is the signature of super-linear
growth---a purely linear step would hold that figure constant.
Normalizing instead by $n\log_2 n$ removes the trend (the figure stays
within a narrow $3.2$--$3.9$ band), and an $n\log n$ fit of the
incremental cost ($R^2 = 0.995$) is closer than a linear one
($R^2 = 0.993$).  Meanwhile the ordered scenario itself remains
approximately linear over the range ($R^2 \geq 0.99$ for a linear
time-versus-size fit).  In short, the empirical results match the
assumed complexity on both sides: linear scaling when the input is
already ordered and the sort is skipped, and an added $O(n\log n)$
term when a genuine sort is required.

% ................................................................
\subsection{Evaluation Validity}
\label{subsec:eval-validity}
% ................................................................

A performance comparison across different systems is only meaningful
if it is constructed so that the measured advantage cannot be an
artifact of the measurement.  The benchmark is designed around that
requirement, and the ways it guards the ``faster'' claim are stated
here explicitly so the result can be judged on its method rather than
on the headline number.

\emph{Correctness precedes speed.}  A lower execution time is only
compared where the systems compute the same answer.  Every reported
cell is checked for row-level output equality against the two SQL
references (Table~\ref{tab:cross_system_correctness}); a system that
returned different or fewer matches would make its timing
incomparable, so the 30/30 output agreement is a precondition for the
timing comparison, not a separate result.

\emph{The timer is bounded in the databases' favour.}  Two deliberate
choices handicap the proposed engine rather than flatter it.  First,
every system receives five warmup runs before measurement, so the
JVM-based Trino is timed in its JIT-warmed steady state rather than
cold; without warmup its first runs are far slower
(Section~\ref{subsec:eval-cross-system}), and reporting those would
overstate the engine's margin.  Second, the reported time excludes the
one-time cost of loading data into each database, measuring only query
execution after loading; this favours the database paths, which
nonetheless remain slower.  A margin that survives both handicaps is
more trustworthy than one obtained by removing them.

\emph{Repetition and low variance.}  Each cell is the mean of twenty
measured runs, reported with its standard deviation, and run-to-run
stability is summarised by the coefficient of variation (about 9\% for
the engine and 2--6\% for the databases,
Section~\ref{subsec:eval-key-findings}).  A single run could be a
fluke; a stable mean over twenty is not.

\emph{Best and worst case, not a cherry-picked scenario.}  The
ordered-input benchmark lets the engine skip sorting and is its best
case; Section~\ref{subsec:eval-ordering} therefore also reports the
shuffled-input worst case, in which every system must sort.  The
engine leads in both, so the advantage is not an artifact of the
ordered-input shortcut.

\emph{A confirmed mechanism.}  The speedup has an attributable
cause---the specialization and compiled fast paths set out in
Section~\ref{subsec:eval-key-findings}---identified by profiling
rather than inferred from the timings alone, so the result does not
rest on an unexplained measurement.

\emph{Stated scope and remaining caveats.}  The claim is deliberately
narrow: the engine is faster \emph{on this workload}---single logical
partition, in-memory input, and the supported feature subset---and is
not presented as a more capable SQL system than Trino or Oracle
(Section~\ref{sec:eval-performance}, ``Positioning'').  Two further
limitations, carried into the threats to validity of
Section~\ref{sec:conc-limitations}, bound the comparison: the database
systems were run at their default container configuration rather than
expert-tuned, so a differently tuned deployment could narrow the gap;
and the results come from one workstation and one dataset.

% ----------------------------------------------------------------
\section{Chapter Summary}
\label{sec:eval-summary}
% ----------------------------------------------------------------

This chapter evaluated the proposed \texttt{MATCH\_\allowbreak{}RECOGNIZE}
implementation through correctness checks, feature-coverage tests,
complexity analysis, performance measurements, memory analysis, and
comparison with existing systems.  The experiments used five pattern
families across six dataset sizes (up to the full 2{,}222{,}742-row
dataset), with every cell reported as the mean of twenty measured
runs after five warmups (with its standard deviation), producing 30
successful benchmark
cells for the proposed engine and a 90-cell cross-system comparison
against Trino~473 and Oracle~XE~21c under the same nominal resource
budget.

The results show that the implementation preserves the evaluated
semantics for the benchmark pattern families---Trino and Oracle
produced output identical to the engine's normalized output in all
30 pattern--size combinations each (all three systems returned
identical result sets)---while reducing
developer effort compared with hand-written Pandas logic.  Under the
selected workload and resource configuration, the proposed engine
sustained stable throughput of roughly 5.0\,M~rows/s with
approximately linear scaling, the lowest average execution time of
the three systems in this benchmark (0.20\,s, versus 0.94\,s for
Oracle and 1.93\,s for a fully-warmed Trino),
query-memory indicator values in the same order of magnitude as the
database engines, and a substantially smaller operational footprint,
since it runs inside a single Python process without a resident
database server.  The next chapter summarizes the thesis
contributions, discusses limitations, and outlines future work.
