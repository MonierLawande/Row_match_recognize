% ============================================================
%  ch3_related_work.tex
%  Chapter 3: Related Work
%  Master's Thesis -- SQL MATCH_RECOGNIZE on Pandas DataFrames
% ============================================================
% !TEX root = Thesis.tex
\chapter{Related Work}
\label{chap:related}

This chapter positions the thesis within existing work on SQL
row-pattern recognition and Python-based tabular analytics.
Section~\ref{sec:related-native} reviews systems that provide native
\texttt{MATCH\_RECOGNIZE} or related row-pattern support.
Section~\ref{sec:related-pandas-gap} discusses the gap in Pandas and
similar DataFrame libraries.  Section~\ref{sec:related-contribution}
summarizes how this thesis addresses that gap.

Row pattern recognition was first proposed as a SQL standard feature
by Zemke et al.~\cite{zemke2007pattern} and was officially adopted
in SQL:2016~\cite{ISO2016SQL,iso2016}.
Subsequent editions refined and documented its
semantics~\cite{ISO2021,melton2017sqlrpr,michels2018new}.
The clause has since been studied both as a specification and in
terms of its practical
implementations~\cite{petkovic2022rpr,petkovic2022fraud}.

The \texttt{MATCH\_RECOGNIZE} clause is supported natively by
several SQL and stream-processing engines, including
Oracle~\cite{oracle2022,book},
Snowflake~\cite{snowflake2025match_recognize}, and
Trino~\cite{trino2022,trino2021row}; Apache Flink also exposes row
pattern recognition through Flink SQL~\cite{flink}.
By contrast, widely used systems such as PostgreSQL and DuckDB do
not currently expose a native \texttt{MATCH\_RECOGNIZE}
implementation~\cite{howe2020matchrecognize,findling2025bringing}.
In systems without native support, users typically emulate
row-pattern matching using window functions, recursive queries,
user-defined aggregates, or external procedural
layers~\cite{TURP2,TURP,cao2012optimizationanalyticwindowfunctions}.
These alternatives can express many of the same ideas, but they are
usually more verbose and harder to maintain than a declarative row
pattern clause.

% ----------------------------------------------------------------
\section{SQL and Stream-Processing Engines with Native Support}
\label{sec:related-native}
% ----------------------------------------------------------------

Oracle~\cite{oracle2022,book}, Trino~\cite{trino2022,trino2021row},
Snowflake~\cite{snowflake2025match_recognize}, and
Flink~\cite{flink} all provide SQL-based row pattern recognition,
but they differ in feature coverage, execution environment, and
practical deployment cost.
Although \texttt{MATCH\_RECOGNIZE} was already defined as part of the
SQL:2016 standard, it has only been implemented in a limited number
of DBMSs to date~\cite{petkovic2022rpr}.
Given the construct's ability to express sequence logic that would
otherwise require verbose combinations of joins, window functions,
or procedural code, it remains useful to extend row-pattern support
to more analytical environments.
Oracle remains a major reference implementation for SQL row pattern
matching~\cite{oracle2022,oracle2021dwhsg,book}.

% ................................
\subsection{Comparative Overview of Existing Implementations}
\label{subsec:related-existing-overview}
% ................................

The SQL standard defines three feature profiles for classifying row
pattern recognition implementations~\cite{ISO2021,michels2018new}.
\textbf{Feature~R010} covers row pattern recognition in the
\texttt{FROM} clause together with basic aggregate functions;
\textbf{Feature~R020} additionally permits row pattern recognition
inside \texttt{WINDOW}/\texttt{OVER} clauses; and
\textbf{Feature~R030} enables extended aggregate support within the
clause.  In practice, Feature~R010 is the common baseline, while
support beyond R010 varies across engines.  Current Trino
documentation, for example, also describes row pattern recognition
inside window structures~\cite{trino2022}.  Feature~R030 is not
widely adopted in publicly documented implementations.

Table~\ref{tab:engine-comparison} summarizes the main systems
considered in this chapter alongside the implementation developed in
this thesis.  The table uses the SQL feature levels where they map
cleanly to public documentation; otherwise, it describes the
documented implementation scope more cautiously.  The comparison is
therefore based on documented feature scope, deployment model, and
research relevance, rather than on a claim that all systems expose
identical SQL semantics.

\begin{table}[htbp]
  \centering
  \caption{Comparison of \texttt{MATCH\_RECOGNIZE} implementations}
  \label{tab:engine-comparison}
  \small
  \setlength{\tabcolsep}{3pt}
  \begin{tabularx}{\textwidth}{l>{\raggedright\arraybackslash}Xcll}
    \toprule
    \textbf{Engine} & \textbf{Documented scope} & \textbf{Open Source} & \textbf{Deployment} & \textbf{License} \\
    \midrule
    Oracle       & \texttt{FROM}-clause row patterns & No  & On-premise / Cloud & Commercial  \\
    Trino        & \texttt{FROM} + window row patterns & Yes & Distributed        & Apache 2.0  \\
    Snowflake    & \texttt{FROM}-clause row patterns + extensions & No  & Cloud-native       & Commercial  \\
    Apache Flink & SQL subset / streaming CEP-backed & Yes & Streaming cluster  & Apache 2.0  \\
    DuckDB       & No native implementation; research translation & Yes & In-process         & MIT         \\
    \midrule
    \textbf{This work} & \textbf{Core R010-style capabilities evaluated here} & \textbf{Yes} & \textbf{In-process Python} & \textbf{Open-source} \\
    \bottomrule
  \end{tabularx}
\end{table}

% ................................
\subsection{Oracle}
\label{subsec:related-oracle}
% ................................

Oracle has been a pioneer in SQL \texttt{MATCH\_RECOGNIZE}, having
introduced row pattern recognition in Database~12c.  It provides a
mature implementation for enterprise-scale relational analytics and
serves as a major reference point in discussions of SQL row pattern
recognition~\cite{oracle2022,oracle2021dwhsg,book}.  Publicly
documented usage focuses on the \texttt{MATCH\_RECOGNIZE} clause in
the table expression context; the window-pattern feature level
(R020) is therefore outside the documented Oracle usage considered
in this thesis.  Oracle also imposes implementation-specific
restrictions, including limitations on Boolean expressions in
\texttt{MEASURES} and restrictions around aggregate syntax.
Despite this technical maturity, Oracle's commercial deployment model
and operational requirements make it less accessible for users seeking
lightweight or open-source experimentation.

% ................................
\subsection{Trino}
\label{subsec:related-trino}
% ................................

Trino is an open-source distributed query engine designed for
parallel, federated query execution across large data
sources~\cite{trino2022,trino2021row}.  Its current documentation
covers \texttt{MATCH\_RECOGNIZE} in the \texttt{FROM} clause and
also documents row pattern recognition in window structures.  This
makes Trino one of the broader open-source implementations from a
SQL-feature perspective, but it is still a distributed query engine
rather than an in-process DataFrame library.  Using it therefore
requires server or cluster deployment and operational management that
may be unnecessary for smaller Pandas workflows.  In this thesis,
Trino is useful primarily as a semantic reference point for
validating selected query results, not as a substitute for a local
DataFrame-native execution path.

% ................................
\subsection{Snowflake}
\label{subsec:related-snowflake}
% ................................

Snowflake is a cloud-native data platform.  It provides documented
\texttt{MATCH\_\allowbreak RECOGNIZE} support with rich
\texttt{DEFINE} and \texttt{MEASURES} expression capabilities for
row-pattern queries~\cite{snowflake2025match_recognize,hoffa2021snowflake}.
Its documentation also notes implementation-specific behavior: the
engine uses backtracking for pattern matching and warns that some
pattern/data combinations can take a long time to execute.  Some
row-pattern functions are also restricted in particular sub-clauses.
Because Snowflake is a managed cloud warehouse, computational cost
scales with warehouse size and query workload.

% ................................
\subsection{Apache Flink}
\label{subsec:related-flink}
% ................................

Apache Flink~\cite{vsprem2024building,georgiou2025streaming} is
designed for real-time stream processing and exposes
\texttt{MATCH\_\allowbreak RECOGNIZE} through Flink SQL, backed by
its Complex Event Processing (CEP) runtime library~\cite{flink}.
Its documented implementation is a SQL subset oriented toward
streaming use cases.  The current Flink documentation explicitly
describes the implementation as a subset of the full standard and
lists unsupported features including pattern groups, some
alternations, \texttt{PERMUTE}, anchors, exclusions,
\texttt{ALL ROWS PER MATCH}, \texttt{SUBSET}, physical offsets
(\texttt{PREV}/\texttt{NEXT}), and \texttt{DISTINCT}
aggregations.  While Flink is highly scalable and effective for
high-velocity data, it assumes a streaming runtime and introduces
its own learning curve for users accustomed to lightweight,
batch-oriented analysis.
For analytical workflows that combine pattern extraction with
machine learning, these systems may also require additional
integration work between SQL execution and Python-oriented modeling
tools~\cite{ML_Workflow,Makrynioti2019sql4mlAD}.  In many practical
pipelines, this can introduce data movement, duplicated
intermediate data, or more complicated orchestration
steps~\cite{naphade2025evolution}.

% ................................
\subsection{Bringing \texttt{MATCH\_RECOGNIZE} to DuckDB}
\label{subsec:related-duckdb}
% ................................

DuckDB is a relational database management system that offers
several advantages over the DBMSs discussed
above~\cite{raasveldt2019duckdb}.
The most significant advantage is that it does not require a
separate server for operation; instead it can be embedded directly
into the host application, making it well-suited for small projects
and in-process analytics.
DuckDB is open-source, enabling users to contribute new
functionality.  Its Python API also makes it convenient to test and
debug generated SQL from translation-based
approaches~\cite{raasveldt2019duckdb}.

Although \texttt{MATCH\_RECOGNIZE} has not yet been implemented in
DuckDB natively, DuckDB supports relevant SQL building blocks such
as window functions and recursive common table expressions.  This
makes it a natural target for research that transpiles
\texttt{MATCH\_RECOGNIZE} queries into standard relational
constructs~\cite{findling2025bringing,mogensen2024compiler,TURP2,TURP}.
Such approaches typically use multiple CTEs and rule-based
rewriting to simulate pattern variables, ordering, skip behavior,
and output measures.  A DuckDB-oriented translation approach would
therefore aim to cover core Feature~R010 use cases without requiring
a native execution operator inside DuckDB itself.

% ................................
\subsection{Implementation Restrictions Across Systems}
\label{subsec:related-restrictions}
% ................................

Existing row-pattern implementations each carry their own
restrictions~\cite{oracle2022,oracle2021dwhsg,trino2022,flink,snowflake2025match_recognize,petkovic2022rpr}:

\begin{itemize}
  \item supported contexts differ---some allow row patterns only in
    the \texttt{FROM} clause, others also in a window;
  \item functions like \texttt{CLASSIFIER()} and
    \texttt{MATCH\_NUMBER()} are not equally available across
    \texttt{MEASURES}, \texttt{DEFINE}, and output modes;
  \item aggregate support varies, including limits on
    \texttt{DISTINCT} aggregates and on aggregate arguments;
  \item empty matches, exclusions, reluctant quantifiers, and skip
    modes are not handled uniformly.
\end{itemize}

For users with limited or occasional needs, standing up Oracle, Trino,
Snowflake, or Flink is heavy--- installing and managing large-scale systems and learning each system's slightly
different syntax for the same query adds hassle~\cite{8509400}.
Table~\ref{tab:engine-comparison} summarizes the systems above.

When the data already fits in memory, Pandas can offer a lower-friction
workflow than a separate server, cluster, or cloud
warehouse~\cite{Evaluation,in-memory_processing}---the gap the next
subsection makes precise.

% ................................
\subsection{Synthesis of the Research Gap}
\label{subsec:related-gap-synthesis}
% ................................

The reviewed systems show row-pattern recognition is a mature, useful
abstraction---but they also expose a deployment and workflow gap.
Oracle and Snowflake are managed or commercial databases, Trino a
distributed open-source SQL engine, Flink a streaming platform.  Each
fits when the data already lives in its environment or the workload
needs distributed processing.  None is convenient when the analyst has
a Pandas DataFrame that already fits in memory and wants to stay in a
Python notebook or script.

So the problem this thesis addresses is not the absence of
\texttt{MATCH\_RECOGNIZE} in SQL engines at large.  It is the absence
of a Pandas-native row-pattern interface that keeps the declarative
structure of SQL row-pattern recognition without a round trip through
an external database or stream processor.  That sets the scope: the
engine targets in-process, offline DataFrame analytics and covers core
R010-style behavior plus selected SQL:2016 constructs---not a
replacement for distributed or streaming engines.

% ----------------------------------------------------------------
\section{Pandas and the Python Ecosystem Gap}
\label{sec:related-pandas-gap}
% ----------------------------------------------------------------

Pandas is a popular open-source Python library for data
analysis~\cite{McKinney2010}.  Easy installation, broad adoption, and
tight integration with the Python data-science stack make it a natural
choice for anyone analyzing tabular data without a separate database.
It offers solid primitives for sorting, filtering, grouping, rolling
windows, shifting columns, and basic time series.

But those are low-level building blocks, not a declarative row-pattern
language.  Expressing a multi-step sequence usually means stitching
together \texttt{groupby}, \texttt{shift}, \texttt{rolling}, custom
loops, or intermediate Boolean columns---code that turns verbose and
error-prone as patterns lengthen, need overlap control, or reference
already-matched rows.

Despite its reach, Pandas has no native SQL:2016
\texttt{MATCH\_\allowbreak RECOGNIZE}-style primitive.  Analysts fall
back on \texttt{DataFrame.\allowbreak apply()} chains,
\texttt{iterrows()}, or database round-trips---all verbose and hard to
maintain.  Within this thesis's scope, other DataFrame libraries (Dask,
Polars, Modin) lack a direct equivalent too, and even DuckDB, with its
in-process analytics and window functions~\cite{findling2025bringing},
does not yet expose a native \texttt{MATCH\_RECOGNIZE} interface.

% ----------------------------------------------------------------
\section{Contribution of This Work}
\label{sec:related-contribution}
% ----------------------------------------------------------------

This thesis closes that gap with five contributions.

\begin{enumerate}
  \item A \textbf{Pandas-native engine} for core SQL:2016
    \texttt{MATCH\_\allowbreak RECOGNIZE} (Feature~R010-style), running
    in-process with no separate database server or cloud warehouse.
  \item \textbf{Broad pattern-construct support}---quantifiers,
    alternation, grouping, anchors, exclusions, and
    \texttt{PERMUTE}---following the main SQL:2016
    \texttt{MATCH\_RECOGNIZE} constructs.
  \item An \textbf{optimization layer}: LRU pattern caching,
    partition-wise memory management, and thread-safe access to shared
    caches.
  \item A \textbf{safe expression-evaluation pipeline} that parses
    user predicates into a controlled representation instead of running
    raw Python, cutting code-injection risk.
  \item \textbf{Empirical validation} on the Amazon UK Products~2023
    catalog (up to 2.2~million rows) and, for scalability, the larger
    Amazon Reviews~2023 corpus (up to 227.9~million rows), with full
    methodology and results in Chapter~\ref{chap:evaluation}.
\end{enumerate}

% ----------------------------------------------------------------
\section{Chapter Summary}
\label{sec:related-summary}
% ----------------------------------------------------------------

This chapter reviewed the main systems that currently support, or
partially support, SQL row-pattern recognition.  Oracle, Trino,
Snowflake, and Apache Flink provide useful reference points, while
DuckDB and Pandas illustrate the current gap for lightweight
in-process analytics.

The chapter showed that existing alternatives either require a
separate database or stream-processing engine, or force analysts to
write procedural logic in Python.  The resulting gap is a scoped but
important one: declarative row-pattern recognition for in-memory
Pandas workflows.  This motivates the system design presented in
Chapter~\ref{chap:methodology}.

% ................................................................
% END OF CHAPTER
% ................................................................
